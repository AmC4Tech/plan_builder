
import { PromptContext } from './types.js';

export const prompts: Record<string, (ctx: PromptContext) => string> = {

  // DOCX: 01立项/01项目建议书/xx项目建议书.docx
  "01立项/01项目建议书/xx项目建议书.docx": (ctx) => `
你是一个专业的项目文档编写助手。
请根据以下章节标题列表，结合项目背景"${ctx.projectData.projectDescription}"，为项目"${ctx.projectData.projectName}"编写对应的内容。
请返回一个 JSON 对象，Key 是章节标题（必须与下方列表完全一致，不要自己加序号），Value 是对应的内容。

要求：
1. **不要** 在 Value 内容中重复包含 Key（标题）本身。
2. **不要** 生成章节目录（TOC）。
3. **绝对禁止** 使用 Markdown 格式。
   - **严禁**出现 \`** \` (粗体)、\` * \` (列表)、\` - \` (列表)、\`1.\` (有序列表) 等符号。
   - **严禁**使用 Markdown 标题符号 (\`#\`, \`##\`)。
   - 输出必须是纯文本。
4. **格式要求**：
   - 避免使用“1、”、“2、”或“1.”、“2.”等序列号，除非原文明确要求。
   - 多个要点请通过自然分段（换行）来区分，或者使用语义化的连接词。
   - 内容应专业、详实，符合商业计划书或项目文档规范。
5. **重要**：对于“项目说明”章节，请严格按照以下格式生成内容：
   - **本项目的名称**：${ctx.projectData.projectName}
   - **项目实施主体**：
     - 如果用户未提供具体客户名，请编造一个合理的客户名称（如“广州市农业局”或“XX科技集团”）。
     - 将开发方/顾问方设定为“本公司”或“XX科技”。
   - **项目性质**：根据项目描述("${ctx.projectData.projectDescription}")概括项目性质。
   - **术语定义**：简要定义“甲方”、“乙方”等术语指代。
   - **禁止**出现“阳江市AA企业”等旧模板中的特定名称。
6. **禁止**生成类似“本文件为项目文档片段…”的免责声明，直接生成正文。
7. **重要**：对于“目 录”章节，Value 值必须为空字符串 ""，**绝对不要**生成任何“此处不生成目录”之类的解释性文字。
8. **重要**：如果父级章节（如“2 项目背景”）与子章节（如“2.1 项目背景”）名称重复，请将父级章节的 Value 设为空字符串，只在子章节生成内容，避免重复。

章节列表:
${(ctx.headers || []).map(h => `- ${h}`).join('\n')}

参考语境（原文档内容，仅供参考风格，请重新生成）：
${(ctx.fileContentPreview || '').substring(0, 500)}...
`,

  // DOCX: 01立项/02项目可行性分析报告/xxx项目可行性研究报告V0.1.docx
  "01立项/02项目可行性分析报告/xxx项目可行性研究报告V0.1.docx": (ctx) => `
你是一个专业的项目文档编写助手。
请根据以下章节标题列表，结合项目背景"${ctx.projectData.projectDescription}"，为项目"${ctx.projectData.projectName}"编写对应的内容。
请返回一个 JSON 对象，Key 是章节标题（必须完全匹配），Value 是对应的内容。

要求：
1. **不要** 在 Value 内容中重复包含 Key（标题）本身。
2. **不要** 生成章节目录（TOC）。
3. **不要** 使用 Markdown 列表格式（如 "- " 或 "* "）来分段。请使用常规的段落文本，段落之间用换行符分隔。
4. 内容应专业、详实，符合商业计划书或项目文档规范。
5. **重要**：如果父级章节（如“2 项目背景”）与子章节（如“2.1 项目背景”）名称重复，请将父级章节的 Value 设为空字符串，只在子章节生成内容，避免重复。

章节列表:
${(ctx.headers || []).map(h => `- ${h}`).join('\n')}

参考语境（原文档内容，仅供参考风格，请重新生成）：
${(ctx.fileContentPreview || '').substring(0, 500)}...
`,

};
